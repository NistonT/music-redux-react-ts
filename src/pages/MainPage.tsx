import { MainSwiper } from "@/widgets";
import { Track } from "@/widgets/Track";

export const MainPage = () => {
  return (
    <div>
      <MainSwiper />
      <Track />
    </div>
  );
};
